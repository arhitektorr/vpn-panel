# Инструкция по переносу VPN ARHITEKTOR Panel на новый сервер

## 📦 Что нужно перенести:

### Обязательные файлы:
- `/opt/vpn-panel/vpn.db` - база данных клиентов
- `/opt/vpn-panel/.env` - переменные окружения
- `/opt/vpn-panel/clientsTable.json` - таблица клиентов
- Ключи из контейнера (приватный и публичный)

### Опционально (если есть в Git):
- Весь код из репозитория

---

## 🔄 СПОСОБ 1: Полный перенос с сохранением ключей

### На СТАРОМ сервере:

```bash
# 1. Создаём бэкап
cd /opt/vpn-panel
./backup.sh

# 2. Копируем бэкап на новый сервер
scp backups/backup_*.tar.gz root@НОВЫЙ_IP:/opt/
